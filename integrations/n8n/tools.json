{
  "_comment": [
    "Which MCP tools this endpoint exposes, and what each one is.",
    "The resource id is what the owner's tiers name, so the split here is the",
    "split she gets to approve. One entry per tool that should be separately",
    "decidable — lumping several into one resource id means she cannot say yes",
    "to one and no to another."
  ],
  "get_invoices": {
    "resource": "billing/get_invoices",
    "scopes": ["invoices:read"],
    "description": "List invoices for a customer"
  },
  "issue_refund": {
    "resource": "billing/issue_refund",
    "scopes": ["refunds:write"],
    "description": "Refund a payment"
  }
}
